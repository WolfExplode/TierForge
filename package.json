{
  "name": "tierforge",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first tier-list builder and importer",
  "scripts": {
    "build": "node scripts/build.js",
    "link:catalog-images": "node scripts/link-catalog-images.js",
    "update:catalogs": "node scripts/update-catalogs.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "serve": "node src/server.js",
    "start": "npm run serve",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/importer.js && node --check src/worker.js && node --check png-metadata.js && node --check app.js && node --check scripts/build.js && node --check scripts/link-catalog-images.js && node --check scripts/update-catalogs.js"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  },
  "engines": {
    "node": ">=20"
  }
}
